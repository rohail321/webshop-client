sudo: required
services:
  - docker
before_build:
  - docker build -t client-client -f Dockerfile.dev .
script:
  - docker run -e CI=true client-client npm test -- --coverage

after_success:
  - docker build -t client-client .
  - echo "$DOCKER_PASSWORD" | docker login  --username "$DOCKER_ID" --password-stdin 
  - docker push client-client

